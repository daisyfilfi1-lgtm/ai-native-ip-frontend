# Netlify Edge Functions 配置
# 
# 使用方法：
# 1. 确保 Railway API 正常运行
# 2. 运行 `netlify deploy --prod` 部署
# 3. 浏览器请求将直接走 Netlify 边缘节点

# Railway API 地址（已在 netlify.toml 中配置，无需在此设置）
# RAILWAY_API=https://ai-native-ip-production.up.railway.app

# 部署命令
# netlify deploy --prod --dir=.

# 或使用 Netlify CLI
# npm install -g netlify-cli
# netlify deploy --prod
